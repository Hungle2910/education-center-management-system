import { TeacherList } from "@/features/teachers/teacher-list";

export const metadata = {
  title: "Quản lý Giáo viên | Education Center CRM",
};

export default function TeachersPage() {
  return <TeacherList />;
}
