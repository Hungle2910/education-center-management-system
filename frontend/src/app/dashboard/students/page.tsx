import { StudentList } from "@/features/students/student-list";

export const metadata = {
  title: "Quản lý Học sinh | Education Center CRM",
};

export default function StudentsPage() {
  return <StudentList />;
}
