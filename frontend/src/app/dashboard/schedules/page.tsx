import { ScheduleCalendar } from "@/features/schedules/schedule-calendar";

export const metadata = {
  title: "Lịch học | Education Center CRM",
};

export default function SchedulesPage() {
  return <ScheduleCalendar />;
}
