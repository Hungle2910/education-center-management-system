import { AdmissionsView } from "@/features/admissions/admissions-view";

export const metadata = {
  title: "Tuyển sinh & Học thử | Education Center CRM",
};

export default function AdmissionsPage() {
  return <AdmissionsView />;
}
