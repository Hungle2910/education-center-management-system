import { ClassList } from "@/features/classes/class-list";

export const metadata = {
  title: "Quản lý Lớp học | Education Center CRM",
};

export default function ClassesPage() {
  return <ClassList />;
}
